
  /student/{pageNo}: 
    get:
      security:
        - Authorization: []
      tags:
      - "Student"
      summary: "To get a list of students"
      description: "This API will allow admin to get a list of students."
      produces:
      - "application/json"
      parameters:
       - in: path
         name: pageNo
         required: true
         description: page number.
         schema:
            type: string
      responses:
        200:
          description: "successful"
        422:
          description: "Unprocessable Entity"
        403:
          description: "Forbidden"
        500:
          description: "Internal Server Error"

  /student/{pageNo}: 
    get:
      security:
        - Authorization: []
      tags:
      - "Student"
      summary: "To get a paginated list of students"
      description: "This API will allow admin to get a list of students"
      produces:
      - "application/json"
      parameters:
       - in: path
         name: pageNo
         required: true
         description: page number of student list.
         schema:
            type: string
       - in: path
         name: pageNo
         required: true
         description: page number.
         schema:
            type: string
      responses:
        200:
          description: "successful"
        422:
          description: "Unprocessable Entity"
        403:
          description: "Forbidden"
        500:
          description: "Internal Server Error"



